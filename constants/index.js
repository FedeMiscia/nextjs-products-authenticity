const contractsAddresses = require("./networkMapping.json")
const nftAddresses = require("./productAddresses")
const nftAbi = require("./productAbi.json")
const marketplaceAbi = require("./marketplaceAbi.json")
const marketplaceAddresses = require("./marketplaceAddresses.json")

module.exports = {
    nftAddresses,
    contractsAddresses,
    nftAbi,
    marketplaceAbi,
    marketplaceAddresses,
}
